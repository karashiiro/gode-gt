package site

// Routes for the Garland Tools API. The string insertion is the slot for the language.
const (
	AchievementIndexPath string = "/db/doc/browse/%s/2/achievement.json"
	CoreDataPath         string = "/db/doc/core/%s/3/data.json"
)
